package com.eliormachlev.currencix.showkase

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.tooling.preview.Preview

// Placeholder so the generated Showkase browser is non-empty on first
// wiring. Every future composable added under src/main/ that carries
// @Preview registers automatically — no per-composable annotation
// needed. Remove this placeholder once real previews land in the
// UI-redesign phases.
@Preview
@Composable
internal fun ShowkasePlaceholderPreview() {
    Text(text = "Showkase ready — add @Preview composables in src/main/")
}
