<?xml version="1.0" encoding="utf-8"?>
<androidx.swiperefreshlayout.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/swipeRefresh"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_display">

        <androidx.coordinatorlayout.widget.CoordinatorLayout
            android:id="@+id/snackbar_top_position"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:elevation="10dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- guideline **************************************************** -->

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/guideline_display"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:orientation="horizontal"
            app:layout_constraintGuide_percent=".45" />

        <!-- refresh indicator ******************************************** -->

        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/refreshIndicator"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:indeterminate="true"
            app:indicatorColor="?attr/colorPrimary"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:trackColor="@android:color/transparent" />

        <!-- toggle button & separator ************************************ -->

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btn_toggle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="40dp"
            android:backgroundTint="?attr/colorPrimary"
            android:contentDescription="@string/desc_toggle_currencies"
            android:onClick="toggleEvent"
            android:src="@drawable/ic_swap_vert"
            app:layout_constraintBottom_toTopOf="@+id/guideline_display"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/guideline_display"
            app:tint="?attr/colorOnPrimary" />

        <!-- fee-amount + cost-with-fee hints ***************************** -->

        <!-- Fee amount hangs directly under textFrom, cost-with-fee under it.
             textFrom's constraints are independent of both, so toggling
             visibility here doesn't shift the input row. -->
        <HorizontalScrollView
            android:id="@+id/scrollViewOriginalFeeAmount"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/margin2x"
            android:scrollbars="none"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/spinnerFrom"
            app:layout_constraintTop_toBottomOf="@id/scrollViewTextFrom">

            <TextView
                android:id="@+id/textOriginalFeeAmount"
                style="@style/TextAppearance.Material3.TitleSmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:accessibilityLiveRegion="polite"
                android:lines="1"
                android:textColor="?attr/colorError"
                android:textDirection="ltr"
                android:textSize="12sp"
                android:visibility="gone"
                tools:text="Conversion fee: 0.25 USD (+ 0.5 %)"
                tools:visibility="visible" />

        </HorizontalScrollView>

        <HorizontalScrollView
            android:id="@+id/scrollViewTrueCost"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/margin2x"
            android:scrollbars="none"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/spinnerFrom"
            app:layout_constraintTop_toBottomOf="@id/scrollViewOriginalFeeAmount">

            <TextView
                android:id="@+id/textTrueCost"
                style="@style/TextAppearance.Material3.TitleSmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:accessibilityLiveRegion="polite"
                android:lines="1"
                android:textColor="?attr/colorError"
                android:textDirection="ltr"
                android:textSize="12sp"
                android:visibility="gone"
                tools:text="Cost with fee: 50.25 USD (+ 0.5 %)"
                tools:visibility="visible" />

        </HorizontalScrollView>

        <!-- exchange rate info text ************************************** -->

        <ImageView
            android:id="@+id/iconHistorical"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:contentDescription="@string/a11y_historical_rate"
            android:paddingTop="1dp"
            android:src="@drawable/ic_history"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/textInfoDate"
            app:layout_constraintEnd_toStartOf="@id/textInfoDate"
            app:layout_constraintTop_toTopOf="@id/textInfoDate"
            app:tint="?attr/colorError" />

        <TextView
            android:id="@+id/textInfoConversion"
            style="@style/TextAppearance.Material3.TitleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:textColor="?android:attr/textColorSecondary"
            android:layout_marginHorizontal="@dimen/margin2x"
            app:layout_constraintBottom_toTopOf="@id/textInfoDate"
            app:layout_constraintEnd_toEndOf="parent"
            android:textSize="12sp"
            tools:text="1 EUR ≈ 11.15 SEK" />

        <TextView
            android:id="@+id/textInfoDate"
            style="@style/TextAppearance.Material3.TitleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/margin2x"
            android:layout_marginVertical="@dimen/margin1x"
            android:gravity="end"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="Rates as of 1/21/22 by Frankfurter.app" />

        <!-- top main_display ********************************************* -->

        <com.eliormachlev.currencix.view.main.spinner.SearchableSpinner
            android:id="@+id/spinnerFrom"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="true"
            app:layout_constraintBottom_toBottomOf="@+id/scrollViewTextFrom"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/scrollViewTextFrom" />

        <!-- EditText (not TextView) so this same field can display the
             formatted expression preview for the custom keypads OR accept
             raw IME input when the "System keyboard" pref is picked.
             Defaults are TextView-shaped and MainActivity flips the input
             flags at runtime when the pref changes. -->
        <EditText
            android:id="@+id/textCalculations"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:autofillHints="off"
            android:background="@null"
            android:cursorVisible="false"
            android:ellipsize="start"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:importantForAutofill="no"
            android:includeFontPadding="false"
            android:inputType="none"
            android:paddingHorizontal="@dimen/margin2x"
            android:singleLine="true"
            android:textDirection="ltr"
            app:layout_constraintBottom_toTopOf="@id/scrollViewTextFrom"
            app:layout_constraintEnd_toEndOf="parent"
            tools:ignore="LabelFor"
            tools:text="13 + 0.99" />

        <!-- textFrom sits centred in the top half regardless of whether
             scrollViewTrueCost is currently visible — anchoring its bottom
             to the guideline instead of chaining to trueCost keeps it from
             re-centring when the fee hint toggles. -->
        <HorizontalScrollView
            android:id="@+id/scrollViewTextFrom"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:scrollbars="none"
            app:layout_constraintBottom_toTopOf="@id/guideline_display"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/spinnerFrom"
            app:layout_constraintTop_toTopOf="parent">

            <!-- EditText (not TextView) so the same field can either display
                 the calculated value for the custom keypads OR accept IME
                 input when the "System keyboard" pref is picked. Defaults are
                 TextView-shaped (no focus, no cursor, no background, no
                 soft-input); textCalculations is the runtime IME target so no
                 fillViewport / tap-stretch is needed here. -->
            <EditText
                android:id="@+id/textFrom"
                style="@style/TextAppearance.Material3.TitleLarge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:autofillHints="off"
                android:background="@null"
                android:cursorVisible="false"
                android:focusable="false"
                android:focusableInTouchMode="false"
                android:importantForAutofill="no"
                android:includeFontPadding="false"
                android:inputType="none"
                android:lines="1"
                android:longClickable="true"
                android:padding="@dimen/margin2x"
                android:textDirection="ltr"
                tools:ignore="LabelFor"
                tools:text="$ 13.99" />
        </HorizontalScrollView>

        <!-- bottom main_display ****************************************** -->

        <com.eliormachlev.currencix.view.main.spinner.SearchableSpinner
            android:id="@+id/spinnerTo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="@+id/scrollViewTextTo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/scrollViewTextTo" />

        <!-- textTo is centred in the bottom half independently of
             scrollViewOriginalValue's visibility. Anchoring its top to the
             guideline (instead of chaining to originalValue) keeps the total
             row stationary when the fee hint toggles. -->
        <HorizontalScrollView
            android:id="@+id/scrollViewTextTo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:scrollbars="none"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/spinnerTo"
            app:layout_constraintTop_toBottomOf="@id/guideline_display">

            <TextView
                android:id="@+id/textTo"
                style="@style/TextAppearance.Material3.TitleLarge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:accessibilityLiveRegion="polite"
                android:lines="1"
                android:longClickable="true"
                android:padding="@dimen/margin2x"
                android:textDirection="ltr"
                tools:text="€ 383.99" />
        </HorizontalScrollView>

        <!-- Value-before-fee sits above reduction-fee, both stacked above
             textTo. textTo's constraints are independent of both, so toggling
             visibility here doesn't shift the total row. -->
        <HorizontalScrollView
            android:id="@+id/scrollViewConvertedFeeAmount"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/margin2x"
            android:scrollbars="none"
            app:layout_constraintBottom_toTopOf="@id/scrollViewTextTo"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/spinnerTo">

            <TextView
                android:id="@+id/textConvertedFeeAmount"
                style="@style/TextAppearance.Material3.TitleSmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:accessibilityLiveRegion="polite"
                android:lines="1"
                android:textColor="?attr/colorError"
                android:textDirection="ltr"
                android:textSize="12sp"
                android:visibility="gone"
                tools:text="Reduction fee: 0.50 EUR (− 0.5 %)"
                tools:visibility="visible" />

        </HorizontalScrollView>

        <HorizontalScrollView
            android:id="@+id/scrollViewOriginalValue"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/margin2x"
            android:scrollbars="none"
            app:layout_constraintBottom_toTopOf="@id/scrollViewConvertedFeeAmount"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/spinnerTo">

            <TextView
                android:id="@+id/textOriginalValue"
                style="@style/TextAppearance.Material3.TitleSmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:accessibilityLiveRegion="polite"
                android:lines="1"
                android:textColor="?attr/colorError"
                android:textDirection="ltr"
                android:textSize="12sp"
                android:visibility="gone"
                tools:text="Value before fee: 100.00 EUR"
                tools:visibility="visible" />

        </HorizontalScrollView>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
